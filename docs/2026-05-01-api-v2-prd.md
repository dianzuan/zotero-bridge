# Zotron XPI API v2 PRD — 2026-05-01

Agent-first JSON-RPC API redesign. Optimized for LLM agent consumers that guess method names and parameter names without reading documentation.

**Design inputs:**
- User feedback from 3 agent sessions (parameter guessing failures, namespace confusion, method-not-found loops)
- Internal audit (82 methods, 36 divergence rows from previous PRD)
- Competitive benchmark against Zotero Web API v3, Notion API, Obsidian Local REST API, Logseq Plugin API, Linear API

**Non-goals:**
- Backward compatibility with v1 method names (skill docs handle migration)
- Convenience aliases / redundant methods (one canonical way per operation)
- Cursor-based pagination (offset is correct for single-writer local DB)

---

## 1. Design Principles

### 1.1 One guess, one hit

An agent holding an item ID and wanting its full text should guess `items.getFullText` — and that should work. The method name, parameter name, and return shape must all match the agent's first instinct.

**Test:** for any user intent, can an LLM produce the correct `namespace.method '{"param": value}'` call without reading docs? If not, the API is wrong.

### 1.2 No cross-namespace dance

An operation that a human would describe as one step must be one RPC call. "Get the PDF text for this paper" should not require `attachments.list` → find PDF → `attachments.getFullText`.

### 1.3 Consistent contracts

- Same parameter name = same semantics everywhere
- Same return envelope for all list operations
- Same confirmation shape for all mutations

### 1.4 Self-describing

The API must be discoverable at runtime via introspection methods. An agent that knows nothing about zotron should be able to call `system.describe` and learn every method, parameter, and return shape.

### 1.5 No redundancy

One canonical method per operation. No aliases, no convenience duplicates. The skill layer teaches agents the correct method names.

---

## 2. Identifier Strategy

### 2.1 Items: `id` accepts `number | string`

All item-scoped methods accept a single `id` parameter that resolves:
1. Numeric Zotero ID: `42`
2. 8-char alphanumeric key: `"YR5BUGHG"` (resolved via `Zotero.Items.getByLibraryAndKeyAsync`)
3. Numeric string: `"42"` (parsed to number, then resolved as numeric ID)

Implementation: `requireItem(idOrKey: number | string)` in `src/utils/guards.ts`.

### 2.2 Collections: `id` accepts `number | string`

All collection-scoped methods accept `id` that resolves:
1. Numeric Zotero ID: `79`
2. 8-char alphanumeric key: `"COL12345"` (resolved via `Zotero.Collections.getByLibraryAndKeyAsync`)
3. Collection name: `"产业经济学年会"` (resolved via name match on `Zotero.Collections.getByLibrary`)

Implementation: extend `requireCollection(idOrKey: number | string)` with same resolution logic as `requireItem`, plus name-based lookup.

### 2.3 Parameter naming

| Parameter | Meaning | Used in |
|-----------|---------|---------|
| `id` | The entity this method operates on | All `get`, `update`, `delete`, `trash`, `restore` methods |
| `parentId` | The parent entity (item or collection) | `notes.list`, `notes.create`, `attachments.list`, `annotations.list`, `collections.create` |
| `ids` | Batch of entity IDs | `batchTrash`, `mergeDuplicates`, export methods |

Never use `itemId`, `collectionId`, `collectionID`, or `relatedId`. When a method takes two items (e.g., add-related), use `id` + `targetId`.

---

## 3. Return Shape Contracts

### 3.1 List envelope

Every method returning multiple entities uses:

```json
{
  "items": [...],
  "total": 166,
  "limit": 25,
  "offset": 0,
  "hasMore": true
}
```

Fields:
- `items`: array of serialized entities (always present, may be empty)
- `total`: total count before pagination (database-side count, NOT slice length)
- `limit`: page size used (default 50 if caller omitted)
- `offset`: starting offset (default 0)
- `hasMore`: `total > offset + items.length`

Applies to: `items.list`, `items.getRecent`, `items.getTrash`, `collections.list`, `collections.getItems`, `notes.list`, `notes.search`, `attachments.list`, `search.quick`, `search.advanced`, `search.fulltext`, `search.byTag`, `search.byIdentifier`, `tags.list`.

**No bare arrays.** Methods that previously returned `[item, item, ...]` must wrap in the envelope.

### 3.2 Single entity

Methods returning one entity return the serialized object directly (no envelope):

```json
{"id": 42, "key": "YR5BUGHG", "title": "...", ...}
```

Applies to: `items.get`, `items.create`, `items.update`, `collections.get`, `collections.create`, `notes.get`, `notes.create`, `notes.update`.

### 3.3 Mutation confirmation

All destructive/state-change operations return:

```json
{"ok": true, "id": 42}
```

The `id` is always the numeric Zotero ID of the affected entity. For batch operations:

```json
{"ok": true, "ids": [1, 2, 3], "count": 3}
```

Applies to: `items.delete`, `items.trash`, `items.restore`, `items.batchTrash`, `collections.delete`, `collections.addItems`, `collections.removeItems`, `tags.add`, `tags.remove`, `tags.rename`, `tags.delete`, `notes.delete`, `attachments.delete`.

No `{deleted: true}`, `{trashed: true}`, `{added: true}`, `{removed: true}`, `{ok: true}` variety. One shape.

### 3.4 Domain-specific shapes (exceptions)

These methods return unique shapes justified by their domain:

| Method | Shape | Justification |
|--------|-------|---------------|
| `items.getFullText` | `{id, attachmentId, content, indexedChars, totalChars}` | Fulltext-specific metadata |
| `items.findDuplicates` | `{groups: number[][], totalGroups}` | Grouped item IDs |
| `collections.tree` | `[{...collection, children: [...]}]` | Recursive tree structure |
| `collections.stats` | `{id, name, items, attachments, notes, subcollections}` | Per-collection counts |
| `attachments.getPath` | `{id, path}` | Simple lookup |
| `annotations.list` | `[{id, type, text, comment, color, pageLabel, position, ...}]` | Annotation-specific fields |
| `export.bibliography` | `{format, html, text, style, count}` | Dual-output (HTML + plain) |
| `export.*` (others) | `{format, content, count}` | Export content |
| `rag.searchHits` | `{hits: [...], total, retrieval: {...}}` | RAG-specific metadata |
| `system.*` | Various | Introspection/meta, heterogeneous by design |

---

## 4. Method Inventory (v2)

### 4.1 items.* (20 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `items.get` | `{id}` | single item | `id` accepts key string |
| `items.list` | `{limit?, offset?, sort?, direction?}` | list envelope | **NEW** |
| `items.create` | `{itemType, fields?, creators?, tags?, collections?}` | single item | — |
| `items.update` | `{id, fields?, creators?, tags?}` | single item | `id` accepts key |
| `items.delete` | `{id}` | `{ok, id}` | `id` accepts key; return `ok` not `deleted` |
| `items.trash` | `{id}` | `{ok, id}` | return `ok` not `trashed` |
| `items.restore` | `{id}` | `{ok, id}` | return `ok` not `restored` |
| `items.getTrash` | `{limit?, offset?}` | list envelope | — |
| `items.batchTrash` | `{ids}` | `{ok, ids, count}` | add `ids` array to response |
| `items.getRecent` | `{limit?, offset?, type?}` | list envelope | **FIX** `total` to true count |
| `items.getFullText` | `{id}` | `{id, attachmentId, content, indexedChars, totalChars}` | **RENAME** from `getFulltext`; auto-resolves PDF attachment from paper ID |
| `items.addByDOI` | `{doi, collection?}` | single item | return single item not array |
| `items.addByURL` | `{url, collection?}` | list envelope | may return multiple items |
| `items.addByISBN` | `{isbn, collection?}` | single item | return single item not array |
| `items.addFromFile` | `{path, collection?}` | single item | return single item not array |
| `items.findDuplicates` | — | `{groups, totalGroups}` | — |
| `items.mergeDuplicates` | `{ids}` | single item | — |
| `items.getRelated` | `{id}` | list envelope | **FIX** wrap in envelope |
| `items.addRelated` | `{id, targetId}` | `{ok, id}` | rename `relatedId` → `targetId` |
| `items.removeRelated` | `{id, targetId}` | `{ok, id}` | rename `relatedId` → `targetId` |
| `items.citationKey` | `{id}` | `{id, citationKey}` | — |

**Removed from items.*:**
- `items.getAttachments` → use `attachments.list`
- `items.getNotes` → use `notes.list`
- `items.getFulltext` → renamed to `items.getFullText` (capital T)

### 4.2 collections.* (12 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `collections.list` | `{limit?, offset?}` | list envelope | **FIX** was bare array |
| `collections.get` | `{id}` | single collection | `id` accepts key/name |
| `collections.getItems` | `{id, limit?, offset?}` | list envelope | `id` accepts key/name |
| `collections.getSubcollections` | `{id}` | list envelope | **FIX** wrap in envelope |
| `collections.tree` | — | `[{...collection, children}]` | — |
| `collections.create` | `{name, parentId?}` | single collection | — |
| `collections.rename` | `{id, name}` | single collection | `id` accepts key/name |
| `collections.delete` | `{id}` | `{ok, id}` | return `ok` not `deleted` |
| `collections.move` | `{id, parentId}` | single collection | rename `newParentId` → `parentId` |
| `collections.addItems` | `{id, ids}` | `{ok, id, count}` | rename `itemIds` → `ids` |
| `collections.removeItems` | `{id, ids}` | `{ok, id, count}` | rename `itemIds` → `ids` |
| `collections.stats` | `{id}` | stats object | — |

### 4.3 notes.* (7 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `notes.list` | `{parentId, limit?, offset?}` | list envelope | **RENAME** from `notes.get` |
| `notes.get` | `{id}` | single note | **NEW** — get single note by ID |
| `notes.create` | `{parentId, content, tags?}` | single note | **FIX** return full item not just `{id, key}` |
| `notes.update` | `{id, content}` | single note | **FIX** return full item |
| `notes.delete` | `{id}` | `{ok, id}` | **NEW** |
| `notes.search` | `{query, limit?}` | list envelope | — |

**Removed from notes.*:**
- `notes.getAnnotations` → moved to `annotations.list`
- `notes.createAnnotation` → moved to `annotations.create`

### 4.4 annotations.* (3 methods) — **NEW NAMESPACE**

Extracted from `notes.*` because annotations are not notes.

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `annotations.list` | `{parentId}` | annotation array | was `notes.getAnnotations` |
| `annotations.create` | `{parentId, type, text?, comment?, color?, position, sortIndex?}` | `{id, key}` | was `notes.createAnnotation` |
| `annotations.delete` | `{id}` | `{ok, id}` | **NEW** |

### 4.5 attachments.* (7 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `attachments.list` | `{parentId, limit?, offset?}` | list envelope | **FIX** wrap in envelope; return `serializeAttachment` (with contentType, path) |
| `attachments.get` | `{id}` | single attachment | **NEW** |
| `attachments.getFullText` | `{id}` | `{id, content, indexedChars, totalChars}` | **RENAME** from `getFulltext`; `id` must be an attachment, not a paper |
| `attachments.add` | `{parentId, path, title?, renameFromParent?}` | single attachment | — |
| `attachments.addByURL` | `{parentId, url, title?}` | single attachment | — |
| `attachments.getPath` | `{id}` | `{id, path}` | — |
| `attachments.delete` | `{id}` | `{ok, id}` | **FIX** return `ok` not old shape |
| `attachments.findPDF` | `{parentId}` | `{attachment: item | null}` | — |

### 4.6 search.* (8 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `search.quick` | `{query, limit?, offset?}` | list envelope | drop `query` echo from response |
| `search.advanced` | `{conditions, operator?, limit?, offset?}` | list envelope | **ADD** `operator: "and" | "or"` (default `"and"`), add `limit`/`offset` |
| `search.fulltext` | `{query, limit?, offset?}` | list envelope | drop `query` echo |
| `search.byTag` | `{tag, limit?, offset?}` | list envelope | drop `tag` echo |
| `search.byIdentifier` | `{identifier, limit?}` | list envelope | add `limit` |
| `search.savedSearches` | — | list envelope | — |
| `search.createSavedSearch` | `{name, conditions}` | `{id, key, name}` | **FIX** add `key` to response |
| `search.deleteSavedSearch` | `{id}` | `{ok, id}` | — |

### 4.7 tags.* (6 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `tags.list` | `{limit?, offset?}` | list envelope | **FIX** wrap in envelope; fix `type: undefined` → `type: 0` |
| `tags.add` | `{id, tags}` | `{ok, id}` | **RENAME** `itemId` → `id` |
| `tags.remove` | `{id, tags}` | `{ok, id}` | **RENAME** `itemId` → `id` |
| `tags.rename` | `{tag, newName}` | `{ok, tag, newName}` | rename `from`/`to` → `tag`/`newName` |
| `tags.delete` | `{tag}` | `{ok, tag}` | — |
| `tags.batchUpdate` | `{ids, add?, remove?}` | `{ok, count}` | rename `itemIds` → `ids` |

### 4.8 export.* (5 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `export.bibtex` | `{ids}` | `{format, content, count}` | **FIX** `throw Error` → `throw {code, message}` |
| `export.cslJson` | `{ids}` | `{format, content, count}` | same fix |
| `export.ris` | `{ids}` | `{format, content, count}` | same fix |
| `export.csv` | `{ids}` | `{format, content, count}` | same fix |
| `export.bibliography` | `{ids, style?, locale?}` | `{format, html, text, style, count}` | — |

### 4.9 rag.* (2 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `rag.searchHits` | `{query, collection?, collectionId?, itemIds?, itemKeys?, limit?, top_spans_per_item?}` | `{hits, total, retrieval}` | — |
| `rag.searchCards` | (same as searchHits) | (same) | compatibility alias; consider removing |

### 4.10 settings.* (4 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `settings.get` | `{key}` | `{key, value}` | **FIX** `key` is required; no multi-shape |
| `settings.set` | `{key, value}` | `{ok, key}` | — |
| `settings.getAll` | — | `Record<string, any>` | — |
| `settings.setAll` | `{settings}` | `{ok, count}` | — |

### 4.11 system.* (13 methods)

| Method | Params | Returns | Change from v1 |
|--------|--------|---------|----------------|
| `system.ping` | — | `{status, timestamp}` | — |
| `system.version` | — | `{zotero, plugin, methods}` | — |
| `system.libraries` | — | library array | — |
| `system.switchLibrary` | `{id}` | `{ok, id}` | — |
| `system.libraryStats` | `{id?}` | stats object | **FIX** `libraryID` → `libraryId` in response |
| `system.itemTypes` | — | type array | — |
| `system.itemFields` | `{itemType}` | field array | — |
| `system.creatorTypes` | `{itemType}` | type array | — |
| `system.sync` | — | `{status}` | — |
| `system.currentCollection` | — | collection or null | **FIX** `libraryID` → `libraryId` |
| `system.reload` | — | `{ok}` | — |
| `system.listMethods` | — | `string[]` | **NEW** — return all registered method names |
| `system.describe` | `{method?}` | method schema(s) | **NEW** — return param schemas + descriptions |

---

## 5. Breaking Changes Summary

### 5.1 Renames

| v1 | v2 | Reason |
|----|-----|--------|
| `notes.get` (list by parent) | `notes.list` | Was a list disguised as get |
| `items.getFulltext` | `items.getFullText` | Casing — agents guess capital T |
| `attachments.getFulltext` | `attachments.getFullText` | Same |
| `notes.getAnnotations` | `annotations.list` | Annotations are not notes |
| `notes.createAnnotation` | `annotations.create` | Same |
| `items.addRelated` `relatedId` | `items.addRelated` `targetId` | Clearer param name |
| `items.removeRelated` `relatedId` | `items.removeRelated` `targetId` | Same |
| `collections.move` `newParentId` | `collections.move` `parentId` | Consistent with other parent refs |
| `collections.addItems` `itemIds` | `collections.addItems` `ids` | Consistent |
| `collections.removeItems` `itemIds` | `collections.removeItems` `ids` | Same |
| `tags.add` `itemId` | `tags.add` `id` | Consistent |
| `tags.remove` `itemId` | `tags.remove` `id` | Same |
| `tags.batchUpdate` `itemIds` | `tags.batchUpdate` `ids` | Same |

### 5.2 Removals

| Method | Replacement |
|--------|-------------|
| `items.getAttachments` | `attachments.list({parentId})` |
| `items.getNotes` | `notes.list({parentId})` |

### 5.3 New methods

| Method | Purpose |
|--------|---------|
| `items.list` | Browse all items with pagination |
| `notes.get` | Get single note by ID |
| `notes.delete` | Delete a note |
| `annotations.list` | List PDF annotations |
| `annotations.create` | Create PDF annotation |
| `annotations.delete` | Delete annotation |
| `attachments.get` | Get single attachment by ID |
| `system.listMethods` | Return all method names |
| `system.describe` | Return method schemas |

### 5.4 Return shape changes

All list methods move from bare arrays to `{items, total, limit, offset, hasMore}` envelope.

All mutation confirmations move to `{ok: true, id}` or `{ok: true, ids, count}`.

---

## 6. Error Strategy

### 6.1 Error shape

```json
{
  "code": -32602,
  "message": "Item 999 not found"
}
```

Codes:
- `-32600` — invalid JSON-RPC request
- `-32601` — method not found
- `-32602` — invalid params (caller's fault: missing item, bad input, wrong type)
- `-32603` — internal error (our fault or Zotero's fault)

### 6.2 Helpful error messages

Error messages should hint at correct usage:

| Bad | Good |
|-----|------|
| `"No arguments provided"` | `"Missing required parameter: parentId"` |
| `"Method not found: items.getNotes"` | `"Method not found: items.getNotes. Did you mean notes.list?"` |
| `"Not an attachment: 5925"` | `"Item 5925 is a journalArticle, not an attachment. Use items.getFullText to auto-find the PDF."` |

### 6.3 Method-not-found suggestions

When `processRequest` encounters an unknown method, fuzzy-match against registered methods and include a `"suggestion"` field:

```json
{
  "code": -32601,
  "message": "Method not found: items.getFullText",
  "data": {"suggestion": "items.getFullText"}
}
```

---

## 7. Introspection

### 7.1 `system.listMethods`

Returns `string[]` of all registered method names, sorted alphabetically.

```json
["annotations.create", "annotations.delete", "annotations.list", "attachments.add", ...]
```

### 7.2 `system.describe`

Without params: returns all methods with their parameter schemas.
With `{method: "items.get"}`: returns schema for one method.

```json
{
  "name": "items.get",
  "description": "Fetch a single item's full metadata by ID or key",
  "params": {
    "id": {"type": "number | string", "required": true, "description": "Numeric ID or 8-char item key"}
  },
  "returns": "Single serialized item object"
}
```

Implementation: store metadata alongside each `registerHandlers` call. The schema object is the source of truth — keep it close to the handler code, not in a separate file.

---

## 8. Skill Layer Responsibilities

The API is clean but agents may still need guidance. The skill docs (`claude-plugin/skills/zotero/`) are responsible for:

1. **Workflow routing** — "want to read a paper? use `items.getFullText`. Want OCR text? use `notes.list` + filter by `ocr` tag. Want RAG spans? use `rag.searchHits`."
2. **Common recipes** — multi-step operations like "OCR → index → search"
3. **Method name teaching** — the skill knows the correct names; agent doesn't need to guess

The skill docs should NOT duplicate parameter schemas (that's `system.describe`'s job) or list all methods (that's `system.listMethods`'s job).

---

## 9. Migration Plan

### Phase 1: Non-breaking additions
- Add `items.list`, `notes.get` (single), `notes.delete`, `attachments.get`
- Add `annotations.*` namespace (keep old `notes.getAnnotations`/`createAnnotation` as deprecated aliases)
- Add `system.listMethods`, `system.describe`
- Extend `requireCollection` to accept key/name
- Fix `items.getRecent` total count

### Phase 2: Renames + return shape normalization
- Rename `notes.get` → `notes.list`
- Rename `getFulltext` → `getFullText`
- Rename parameter names (`itemId` → `id`, `relatedId` → `targetId`, etc.)
- Wrap all bare-array returns in envelope
- Normalize mutation returns to `{ok, id}`

### Phase 3: Removals + cleanup
- Remove `items.getAttachments`, `items.getNotes`
- Remove deprecated annotation aliases from `notes.*`
- Remove `rag.searchCards` if no consumers
- Update all skill docs to match v2 names

Each phase is one commit on dev. Phase 2 is the breaking change — bump minor version. Skill docs update in same commit.
